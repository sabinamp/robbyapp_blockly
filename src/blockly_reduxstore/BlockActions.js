import {
  UPDATE_BLOCK, DELETE_BLOCK, ADD_BLOCK, NAME_BLOCK, LOAD_BLOCKS
} from './actionTypes'
/*  const initialState = {
  blocks: []
export default function blocks(state = initialState, action = {}) {
  switch (action.type) {
    case LOAD_BLOCKS:
      return action.results || [];

    case ADD_BLOCK:
      return [
        ...state,
        action.block
      ]
    case NAME_BLOCK:
        return [
          ...state,
        action.name
      ]
   default:
      return state;
  }
};   */
